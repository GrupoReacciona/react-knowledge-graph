// react-knowledge-graph: public API surface for @gruporeacciona/react-knowledge-graph.
// The real `KnowledgeGraphViewer` component, hooks (useGraphSelection, useGraphSearch,
// useGraphFilters, useGraphCamera, useGraphStats), and optional panels land in Phase 3
// (VIEWER-01..10). This is a placeholder entry point so the package has a valid,
// non-empty compilation unit for `tsc --build` during Phase 1 scaffolding.
export {};
