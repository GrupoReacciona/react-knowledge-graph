// adapters/codebase-memory: converts Codebase Memory MCP's internal graph
// format into the neutral NormalizedGraph model (graph-core).
//
// The real conversion function (fromCodebaseMemory()) is Milestone 6 /
// ADAPT-01 (v2, deferred) — per D-10, no logic lands here in Phase 1.
// This placeholder exists solely to give TypeScript's composite build a
// non-empty `include: ["src"]` compilation unit (an empty `src/` directory
// errors with "No inputs were found").
export {};
